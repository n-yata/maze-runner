# タスクリスト: バグ修正

## Bug 2: 壁すり抜け修正
- [x] `src/ghost.ts` の `chooseDirection()` に行き止まりフォールバック（`bestDist === Infinity` のとき `opp` を返す）を追加

## Bug 1: 衝突判定修正
- [x] `src/ghost.ts` の `update()` 衝突チェックをピクセル距離ベースに変更（TILE_SIZE * 0.75 = 12px）
- [x] 移動後にも衝突チェックを追加（`moveGhost()` 呼び出し後）

## テスト追加
- [x] `tests/unit/ghost.test.ts` に壁すり抜けシナリオのテスト追加
- [x] `tests/unit/ghost.test.ts` に衝突判定（ピクセル近接）のテスト追加

## 検証
- [x] `npm test` 全テストパス（106件）
- [x] `npm run lint` エラーなし
- [x] `npm run typecheck` エラーなし

---

## 申し送り事項

**実装完了日**: 2026-05-28

### 実装内容
- `src/ghost.ts` のみ変更（テスト含む）

### 修正の詳細

**Bug 1（衝突判定）**:
- 旧: タイル座標完全一致のみ判定
- 新: ピクセル距離 < TILE_SIZE * 0.75（12px）で判定 + moveGhost後に再チェック
- 根本原因は「プレイヤー移動 → ゴースト衝突チェック → ゴースト移動」の順序で、同フレーム内にすれ違いが起きていた

**Bug 2（壁すり抜け）**:
- 旧: 行き止まりで全方向がスキップされると `bestDir = g.dir`（壁方向）が返される
- 新: `bestDist === Infinity` の場合に `opp`（後退）を返す
- FRIGHTENEDモードには同様の処理があったが通常モードに欠けていた

### 計画との差分
特になし。設計通りに実装できた。

### 次回への改善提案
- 衝突判定の閾値（TILE_SIZE * 0.75 = 12px）は定数化して調整しやすくすることを検討
- ゴーストがEATENモードでゴーストハウスに帰着後の復活ロジックが未実装（別バグ候補）

